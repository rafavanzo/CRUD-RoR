<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% flash.each do |type, message| %>
      <p class="py-2 px-3 mb-5 font-medium rounded-lg inline-block <%= type == 'notice' ? 'bg-green-50 text-green-500' : 'bg-red-50 text-red-500' %>" id="<%= type %>"><%= message %></p>
    <% end %>

    <h1 class="font-bold text-4xl"><%= t("pessoas.form.title") %></h1>

    <%= render @pessoa %>

    <%= link_to t('pessoas.form.edit'), edit_pessoa_path(@pessoa), class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to t('pessoas.form.back'), pessoas_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to t("pessoas.form.destroy"), confirm_delete_pessoa_path(@pessoa), class: "ml-2 rounded-lg py-3 px-5 bg-red-600 text-white inline-block font-medium" , data: { confirm: t("pessoas.form.confirm_delete") } %>
  </div>
</div>